import type * as expressSession from 'express-session';
export declare const unlockConvictionBatch: (batchID: number, requestSession: expressSession.Session) => boolean;
export default unlockConvictionBatch;
