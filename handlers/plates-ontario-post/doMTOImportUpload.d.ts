import type { RequestHandler } from "express";
export declare const uploadHandler: RequestHandler<import("express-serve-static-core").ParamsDictionary, any, any, any>;
export declare const handler: RequestHandler;
